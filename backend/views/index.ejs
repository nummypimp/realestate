<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ดาวน์โหลดวิดีโอ TikTok</title>
  <style>
    body {
      font-family: sans-serif;
      background: #fafaff;
      padding: 20px;
      margin: 0;
    }
    h1 {
      font-size: 1.5em;
      margin-bottom: 10px;
    }
    form {
      margin-bottom: 20px;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }
    input[type="text"] {
      width: 100%;
      max-width: 500px;
      padding: 10px;
      font-size: 16px;
    }
    button {
      padding: 10px;
      font-size: 16px;
      background-color: #2d8cf0;
      color: white;
      border: none;
      border-radius: 5px;
      width: fit-content;
    }
    .error {
      color: red;
      margin-bottom: 10px;
    }
    video {
      width: 100%;
      max-width: 500px;
      border-radius: 8px;
    }
    a {
      color: #2d8cf0;
    }
  </style>
</head>
<body>
  <h1>ดาวน์โหลดวิดีโอ TikTok (ไม่มีลายน้ำ)</h1>
  <p><a href="/search">ค้นหาคลิปจาก Keyword</a></p>

  <form method="POST" action="/download">
    <input type="text" name="tiktokUrl" placeholder="วางลิงก์ TikTok ที่นี่" required>
    <button type="submit">ดาวน์โหลด</button>
  </form>
  <% if (error) { %>
    <div class="error"><%= error %></div>
  <% } %>
  <% if (videoUrl) { %>
    <div>
      <video src="<%= videoUrl %>" controls width="400"></video>
      <br>
      <a href="<%= videoUrl %>" download>ดาวน์โหลดไฟล์วิดีโอ</a>
    </div>
  <% } %>
</body>
</html>
